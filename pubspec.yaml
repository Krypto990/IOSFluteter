name: linkseguro
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.1.2
  url_launcher: ^6.0.9 # Reemplaza con la última versión
  qr_code_scanner: ^1.0.1
  http: ^1.2.2
  sqflite: ^2.0.0+3
  path_provider: ^2.0.9
  
  # Otras dependencias aquí


dev_dependencies:
  flutter_test:
    sdk: flutter
    

flutter:
  uses-material-design: true

  assets:
    - assets/newAssets/
